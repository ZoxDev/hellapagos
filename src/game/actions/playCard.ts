import type { CrashedBoatCard } from "../cards/crashedBoatCards";

export const playCard = ({
	card,
	globalFood,
	globalWater,
}: {
	card: CrashedBoatCard;
	globalFood: number;
	globalWater: number;
}) => {};
